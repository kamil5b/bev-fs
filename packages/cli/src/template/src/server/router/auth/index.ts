/**
 * Auth Router
 * File-based routing for authentication endpoints
 * Routes are auto-discovered in subdirectories:
 * - POST /auth/login
 * - POST /auth/signup
 * - POST /auth/logout
 */

export default {}
